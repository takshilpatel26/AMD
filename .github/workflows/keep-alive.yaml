name: Keep Render Awake

on:
  schedule:
    - cron: "*/4 * * * *"   # every 4 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl health endpoint
        run: |
          curl -s https://amd-4gq6.onrender.com/health/ > /dev/null